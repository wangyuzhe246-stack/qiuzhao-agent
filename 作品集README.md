# 🎓 秋招智能投递助手

> **个人项目 · 用 AI Agent 把「找岗 → 匹配 → 定制材料 → 跟踪投递」串成一个闭环**
> 面向 2027 届毕业生的秋招场景，由我独立设计、开发并真实使用。

🔗 **在线演示（无需安装，点开即看）**：将 `demo/index.html` 部署到 GitHub Pages / Netlify 后填入链接，例如 `https://你的用户名.github.io/qiuzhao-agent/`

---

## 一、一句话介绍

一个跑在本地浏览器里的 **AI 求职工作台**：输入一次我的简历和偏好（央国企优先、目标北上广深杭），AI 就自动联网搜索开放中的 2027 届秋招岗位，按匹配度打分、识别福利待遇、去重整理，再为每个确认的岗位**按 JD 定制**简历、自我评价、网申答案和投递指引——全程数据留在本地。

## 二、项目背景（我为什么做这个）

2027 届秋招季，作为**双非二本、电子商务专业**的毕业生，我面对的真实痛点：

- **信息分散**：国聘网、企业官网、智联/前程无忧、各省人才网……岗位散落在几十个渠道，手动刷 JD 效率极低；
- **简历千篇一律**：每个公司、每个岗位的要求都不同，手写定制一份简历要 1-2 小时；
- **投递容易漏**：几十个岗位的截止日期、进度、材料，靠表格和笔记根本管不过来。

我的解法不是"再努力一点手动刷"，而是**写一个 AI Agent 把重复劳动自动化**——把时间留给真正重要的事（了解行业、准备面试、打磨表达）。

## 三、核心功能

| 功能 | 说明 |
|---|---|
| 🌐 **AI 多轮联网找岗** | 11 轮定向搜索，优先 30 个官方/招聘渠道（国聘、24365、企业校招官网等），返回岗位+真实投递入口 |
| 📊 **岗位匹配打分（0-100）** | 公司类型 40 + 关键词 30 + 学历 10 + 城市 20 四维打分；央国企优先，其次大厂 |
| 🧑‍💻 **AI 职业画像（职业记忆库）** | 从简历提炼职业定位/核心能力/行业经验/优势标签/成果/职业目标/目标岗位，可编辑，作为匹配分析的大脑 |
| 🎯 **Job Intelligence 匹配分析 + 综合岗位价值评分** | 粘贴 JD 全文或回退现有信息 → AI 语义理解 + 岗位解释（实际做什么/日常/发展）→ 简历匹配度（**结构化 40% + 语义 60%**）→ **应届生秋招视角**再评 岗位价值/成长空间/竞争难度 三维 + 平台分 → **A/B/C/D 分层推荐**（重点投递/重点冲刺/探索投递/减少投入）→ 推荐理由/风险提示逐条给到 |
| 🎁 **福利识别与评级** | 自动识别 薪资/双休/五险一金或六险一金/房补餐补路补，并按「五险一金+双休+补贴齐全=优」规则评级 |
| 🔄 **近似岗位去重** | 规范化公司名 + 岗位关键词重叠判断，把同一岗位不同措辞合并，63 个近似岗合并到 41 个不重不漏 |
| 📝 **按 JD 定制投递材料** | 一个岗位一套：定制简历 / 自我评价 / 网申答案 / 自我介绍 / 投递指引，可在线编辑、导出 |
| 📋 **投递跟踪看板** | 待投递 → 已投递 → 笔试 → 面试 → 结果 全流程进度管理 |

## 四、真实运行成果（数据说话）

截至 2026-08，使用本工具完成一轮真实秋招找岗：

| 指标 | 结果 |
|---|---|
| 有效岗位数 | **41 个**（原始 63 个近似岗位去重后） |
| 央国企 + 大厂岗位占比 | **≈ 83%**（国企 16 + 大厂 18，符合"央国企优先"偏好） |
| 平均匹配分 | **77 分**（最高 91） |
| 福利「优」岗位 | **20 个**（五险一金 + 双休 + 补贴齐全） |
| 覆盖企业 | 字节跳动、岚图汽车（东风）、中建八局文旅、中旅/华侨城系等 |

> 完整岗位数据见 `demo/` 演示页（已内置真实数据，可搜索、可筛选）。

## 五、技术栈与系统架构

```
浏览器（agent.html 原生单页工作台）
        │  HTTP / JSON（仅本机 127.0.0.1）
        ▼
Node.js + Express（server.js + routes.js）
        │
        ├─ llm.js        DeepSeek Responses API（deepseek-v4-flash，内置 web_search 联网）
        ├─ jobSearch.js  多轮搜索 → 打分 → 模糊去重 → 入库   ◀ 发现层
        ├─ profile.js    简历 → 职业画像（职业记忆库）
        ├─ jdIntel.js    JD 语义理解 + 岗位解释 + 三维评分（价值/成长/竞争）+ 推荐理由/风险
        ├─ match.js      混合评分（结构化40%+语义60%）+ 综合岗位价值评分 + A/B/C/D 分层 → applications.json
        ├─ jobDiscovery.js 预留 Job Discovery Agent 接口（未来主动发现岗位）
        ├─ tailoring.js  按 JD 生成定制材料
        ├─ resume.js     PDF/DOCX 解析 + AI 结构化
        └─ store.js      data/*.json 本地原子读写
```

- **后端**：Node.js + Express，仅绑定 `127.0.0.1`，本地隐私可控；
- **AI 引擎**：DeepSeek API（`deepseek-v4-flash`），内置 web_search，一次找岗约 1-3 次联网调用，整个秋招成本约 ¥10-50；
- **前端**：原生 HTML/CSS/JS 单页应用，无框架、无构建，双击即用；
- **存储**：本地 JSON 文件（岗位/简历/材料/设置），不引入数据库依赖；

## 六、关键设计与难点（面试可以聊的点）

1. **LLM 输出的稳定性**：直接 JSON 解析失败时，自动回退到带格式约束的二次调用 + 兜底清洗，保证找岗结果稳定入库。
2. **Job Intelligence 混合评分**：不纯靠 LLM。`结构化 40%` 用代码规则把画像能力项（核心能力+技能+优势标签）与 JD 文本做字符二元组软匹配（解决"达人营销与BD" vs"达人合作进行营销BD"的措辞差异），确定性、零成本、可解释；`语义 60%` 由 LLM 结合画像事实逐条给理由。分太低时用户能看出是 JD 信息太薄，引导补充全文。
3. **JD 语义理解 + 岗位解释**：一份薄薄的 JD 摘要（如"运营方向"）被解析成事业群/具体岗位/招聘状态/发布时间，并用"这个岗位实际做什么 / 日常工作 / 发展路径"讲给求职者听——让"用户输入 JD 全文"成为最高质量的输入路径，页面抓取主动降级。
4. **应届生秋招分层推荐（A/B/C/D）**：只输出"简历匹配度"会误伤"匹配 70-80% 但高成长/高平台"的岗位。升级为**综合岗位价值评分**（0.35×匹配 + 0.25×价值 + 0.20×成长 + 0.10×(100−竞争) + 0.10×平台），LLM 一次性返回三维评分 + 推荐理由 + 风险提示（每岗仍是 1 次调用，成本不变），代码规则负责平台分、分层（`match≥70 且 value/growth≥70→A；match≥55 且 value/growth≥75→B；价值高或平台高但匹配一般→C 探索；其余→D`）与综合分合成——确定性、零成本、可解释，旧档案靠"跳过条件改为检查 rec.recScore"就地迁移。
5. **福利信息抽取与评级**：把「六险二金」「双休（做五休二）」「房补/餐补」这类非结构化文本规范化，再用规则判定等级——一开始"六险二金"会被误判为中，改成按险种/公积金字段语义匹配后回归通过。
6. **模糊去重**：同一岗位在不同平台措辞不同（如"运营方向（2027校园招聘）"vs"运营管培生"）。实现"公司名规范化 + 岗位关键词 token 重叠/核心包含"双重判断，去重后岗位质量不降、不漏。
7. **Prompt 工程**：11 轮搜索分别面向不同渠道和偏好，搜索词里显式带上"2027届""秋招""网申"等时效词，提高命中率。
8. **产品思维**：坚持"半自动"——招聘平台有验证码和风控，全自动无人值守投递不可靠，所以让 AI 完成"找、想、写"，把"提交"留给人手动完成，既负责又务实。

## 七、如何运行

```bash
npm install        # 首次安装依赖
node server.js     # 启动，自动打开浏览器 http://127.0.0.1:8787
```

Windows 用户直接双击 `启动.bat` 即可。进入设置页填入 DeepSeek API Key 即可使用（申请：platform.deepseek.com）。

## 八、目录结构

```
├── server.js            # Express 入口 + 静态托管
├── src/
│   ├── llm.js           # DeepSeek 封装：联网找岗 / 定制材料 / 稳定解析 JSON
│   ├── jobSearch.js     # 多轮搜索、打分、模糊去重（发现层）
│   ├── profile.js       # 简历 → AI 职业画像（职业记忆库）
│   ├── jdIntel.js       # JD 语义理解 + 岗位解释 + 三维评分 + 推荐理由/风险
│   ├── match.js         # 混合评分 + 综合岗位价值评分 + A/B/C/D 分层 → applications.json
│   ├── jobDiscovery.js  # 预留 Job Discovery Agent 接口（未来主动发现）
│   ├── tailoring.js     # 按 JD 定制投递材料
│   ├── resume.js        # 简历解析
│   ├── store.js         # JSON 本地存储
│   └── routes.js        # REST 接口
├── scripts/
│   └── build-demo.mjs   # 由真实数据生成作品集演示页（本次新增）
├── demo/index.html      # 作品集静态演示页（真实数据，无需后端）
├── agent.html / app.js / style.css   # 前端
├── data/                # 本地数据（岗位/简历/材料/设置，已 gitignore）
└── 启动.bat / 使用说明.md
```

## 九、后续规划

- 增加"每日增量刷新"：自动追踪新岗位和截止日期临近提醒；
- 覆盖更多国企专项平台（国家电网、中智招聘等）的定向搜索模板；
- 投递材料质量评估（用 AI 自评"这版简历和 JD 的匹配度"再迭代）。

## 十、关于作者

双非二本 · 电子商务专业 · 2027 届，求职方向：**央国企/大厂 数字化运营、内容运营、市场类岗位**。

- 这个项目教会我的：**遇到问题先问"能不能自动化"，再动手写代码；以及用 AI 提升自己的工作效率，而不是等着 AI 替我做决定。**
- 联系方式：153575379@qq.com
